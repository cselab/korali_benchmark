#!/bin/bash

#echo "$1 - $2"
sampleId=`cat $1 | tail -n 1 | rev | cut -d ' ' -f 2`
mean=750
var=500
rndNumber=$(( $mean + $RANDOM % $var ))
waitTime=`echo "$rndNumber / 1000" | bc -l`
sleep $waitTime
#echo "Random Number: $rndNumber"
echo $sampleId > application.out

